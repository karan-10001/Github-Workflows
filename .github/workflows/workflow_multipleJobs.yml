name: My first workflow

on: 
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  
jobs:
  UI_Tests:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code 
        uses: actions/checkout@v4

      - name: print hello
        run : echo "hellow world"

      - name: print bye bye
        run : echo "bye bye bro!"
  
  API_Test:
    runs-on: ubantu-latest

    steps: 
      - name: checkout code 
        uses: actions/checkout@v4
        
      - name: print this 
        run: echo "this is api test job"

      - name: print bye bye 
        run: echo "bye bye "
